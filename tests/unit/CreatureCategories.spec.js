import { shallowMount } from "@vue/test-utils";
import CreatureCategories from "@/components/CreatureCategories.vue";

describe("CourseForm.vue", () => {
  it("If the category check-boxes are there", async () => { });
  it("If the categorie check-boxes trigger select event (changing false -> true)", async () => { });
  it("If the categorie check-boxes trigger deselect event (changing true -> false)", async () => { });
  it("If the select event (changing false -> true) emits", async () => { });
  it("If the deselect event (changing true -> false) emits", async () => { });
  it("If check-boxes get colored if selected", async () => {});
  it("If check-boxes get dis-colored if deselected", async () => {});
});
