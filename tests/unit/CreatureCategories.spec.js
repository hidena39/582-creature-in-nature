import { shallowMount } from "@vue/test-utils";
import CreatureCategories from "@/components/CreatureCategories.vue";

describe("CourseForm.vue", () => {
  it("If the category buttons are there", async () => {});
  it("If buttons get colored if selected", async () => {});
  it("If buttons get dis-colored if deselected", async () => {});
  it("If the selected categories make an array in data", async () => {});
  it("if the page lists the cards with the selected categories", async () => {});
});
