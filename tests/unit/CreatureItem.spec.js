import { shallowMount } from "@vue/test-utils";
import CreatureItem from "@/components/CreatureItem.vue";

describe("CourseItem.vue", () => {
  it("renders the props", () => {});
  it("shows the detail button", () => {});
  it("emits an sendAShowDetail event when the card is clicked by two way binding", () => {});
  it("emits a sendHideDetail event when back-to-card is clicked by two way binding", () => {});
});
