import { shallowMount } from "@vue/test-utils";
import CreatureDetail from "@/components/CreatureItem.vue";

describe("CourseDetail.vue", () => {
  it("emits sendAShowDetail event when detail button is clicked in CreatureItem.vue by two way binding", async () => {});
  it("emits sendAHideDetail event when back-to-card button is clicked by two way binding", async () => {});
  it("shows a detail card (with back-to-card button) if it is selected", () => {});
  it("shows the back-to-card button when the detail card is selected", () => {});
  it("hides detail card (with back-to-card button) if it is not selected", () => {});
});
