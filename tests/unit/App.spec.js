import { shallowMount } from "@vue/test-utils";
import App from "@/App.vue";

describe("App.vue", () => {
  it("If it receive the data from Input.spec.js", () => {});
  it("If it add cards with the information typed in Input.spec.js", () => {});
});
